# 全局私密参数示例文件（不要提交 .env 到仓库）
# 复制为 .env 并填入你自己的密钥：
#   cp .env.example .env

# OpenRouter / LLM
OPENROUTER_API_KEY=your-openrouter-api-key

# OpenAI / 兼容端点（如需使用 Whisper / TTS / 审核）
OPENAI_API_KEY=your-openai-api-key

# ElevenLabs（可选，用于高质量 TTS）
ELEVENLABS_API_KEY=your-elevenlabs-api-key
