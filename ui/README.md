# ui 目录说明

职责：基于 PySide6 的桌面界面，包括主窗口与可复用组件。

- `main_window.py`：主窗口/应用入口 UI 逻辑。
- `ui_components/`：可复用的控件或复合组件。

开发要点：
- UI 层只做展示与用户交互，业务流程通过 `core` 的服务/节点调用。
- 避免在 UI 里直接请求外部服务，需经封装层（如 `core/api/llm.py`）。
- 事件与槽函数保持短小，必要时抽取到辅助类/函数。
- 长耗时或 I/O 操作避免阻塞 UI 线程，可使用异步/线程并在完成后调度回主线程更新界面。

